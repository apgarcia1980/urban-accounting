// Set the verified public origin before publishing; localhost is deliberate in phase one.
export const siteConfig = {
  name: 'Urban Accounting',
  origin: 'http://localhost:4200',
  indexable: false,
} as const;
